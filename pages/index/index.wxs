var getA = function(a) {
  return a % 2 === 0 ? '偶数' : '奇数'
}

var getTodos = function(todos) {
  return todos.map(function(n) {
    n.sex = 1
    return n
  })
}

module.exports = {
  getA: getA,
  getTodos: getTodos
};
